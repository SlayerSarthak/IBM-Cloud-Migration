 - Replacement for the Infra_migration Script : 

1. Source IP Mapping (IMPORTANT)
src_ip = "REPLACE_WITH_SOURCE_IP"

👉 Replace with actual mapping:

VM1 → source IP1
VM2 → source IP2

🔴 2. Credentials
API_KEY = "your-api-key"
RESOURCE_GROUP_ID = "your-resource-group"

🔴 3. SSH Key
"ssh_key_path": "/path/to/key.pem"
